#pragma once

// Automatically generated using enum_gen.py

typedef enum {
  AP_STATE_DISCONNECTED = 0,
  AP_STATE_CONNECTING = 1,
  AP_STATE_CONNECTED = 2,
  AP_STATE_PINGED = 4,
  AP_STATE_MAX = 5,
} ap_state_t;
